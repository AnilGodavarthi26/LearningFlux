apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mysql
  namespace: default
spec:
  interval: 1m
  chart:
    spec:
      chart: mysql
      version: "14.0.3"
      sourceRef:
        kind: HelmRepository
        name: mysql
        namespace: flux-system
      interval: 1m
  values:
    auth:
      username: "anil"
      password: "pass"
      database: "mydb"


apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mysql
  namespace: default
spec:
  interval: 1m
  chart:
    spec:
      chart: mysql
      version: 9.10.9
      sourceRef:
        kind: OCIRepository
        name: mysql
        namespace: default
      interval: 1m
  values:
    auth:
      username: "anil"
      password: "pass"
      database: "mydb"

